@*@model IEnumerable<WebQuanAoDoAn.Models.KICH_THUOC>*@
@using WebQuanAoDoAn.Models
@using PagedList.Mvc;
@model PagedList.PagedList<KICH_THUOC>

@{
    ViewBag.Title = "QLsize";
    Layout = "~/Views/Shared/layoutADMIN.cshtml";
}

<h2>QLsize</h2>

<p>
    @Html.ActionLink("Create New", "Themmoisize")
</p>
<table class="table">
    <tr>
        <th>
            TÊN SP:
        </th>
        <th>
            TÊN size:
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SAN_PHAM.TEN_SP)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TEN_KICH_THUOC)
            </td>
            <td>
                @Html.ActionLink("Edit", "editsize", new { id = item.MA_KICH_THUOC }) |
               
                @Html.ActionLink("Delete", "DeleteSize", new { id = item.MA_KICH_THUOC })
            </td>
        </tr>
    }

</table>
<div>
    Trang @(Model.PageNumber>Model.PageCount?0:Model.PageNumber) /@Model.PageCount
</div>
<div class="Menutrang">
    @Html.PagedListPager(Model, page => Url.Action("Qlsize", new { page = page }))
    <a href="/Admin/IndexAdmin">QUAY LAI</a>
</div>

<style>
    .Menutran li {
        display: inline
    }
</style>